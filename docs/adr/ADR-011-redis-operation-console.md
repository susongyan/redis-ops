# ADR-011：结构化 Redis 操作终端

## 状态

已接受（Phase 3 质量治理增强）

## 决策

平台采用版本化 MySQL 命令目录和结构化参数表单，不暴露任意 Redis CLI/RESP。操作按风险等级执行确认或审批，所有写操作在 Redis 连接 Provider 统一获取连接信息，并保留可审计的操作状态。

## 原因与边界

命令目录让参数校验、风险策略和前端表单可配置；Redis 执行器仍只实现明确支持的单 Key 命令，未知命令 fail-closed。复杂命令通过后续适配器扩展，不允许以“配置化”绕过路由、审批或安全限制。
